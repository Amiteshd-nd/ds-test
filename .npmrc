# Use a hoisted node_modules layout. Keeps Next.js, the better-sqlite3 native
# module, and Phaser resolving the way they did before the monorepo — avoids
# pnpm's strict-symlink edge cases in this mixed Vite + Next repo.
node-linker=hoisted
